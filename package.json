{
  "name": "graphql-learning-1",
  "author": "",
  "version": "1.0.0",
  "description": "graphql learning api",
  "main": "index.js",
  "scripts": {
    "serve": "babel src --out-dir dist/ && node dist/serve.js",
    "build": "babel src --out-dir dist/ && node dist/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.0",
    "express-graphql": "^0.12.0",
    "graphql": "^15.8.0"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-plugin-transform-object-rest-spread": "^6.26.0",
    "babel-preset-env": "^1.7.0"
  }
}
